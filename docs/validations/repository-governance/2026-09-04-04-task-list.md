# Repository Governance — Task-List Revalidation

Version: 1.0  
Date: 2026-09-04  
Work ID: `repository-governance`  
Result: Pass with residual risk  
Evaluator: Independent Codex subagent

Validated artifact: [Implementation task list version 0.3](../../plans/repository-governance/tasks.md)  
Validated SHA-256: `f4fa90dd288aacfccccedf18a25a3098f5cbc6d4944a436bf629e8f16b6387d8`

This report follows the immutable [version 0.2 task-list validation](2026-09-04-02-task-list.md) and responds to finding V2 in the immutable [failed implementation validation](2026-09-04-03-implementation.md). It does not change or replace either earlier report. It validates the later task-list artifact.

This report becomes immutable when it first enters the default branch.

## Method

The evaluator:

- Confirmed that the supplied draft SHA-256 was `9725847aeefe4c8da7b89c0489680babe5f67405240d340d4a211e22fa18334f` before the permitted validation metadata update.
- Compared task-list version 0.3 with specification version 0.2, the earlier task-list validation, and the failed implementation validation.
- Parsed all 45 task rows and checked column count, task IDs, dependency references, dependency order, and status conditions.
- Checked the status and evidence for all work after blocked task 1.3.
- Checked that task 4.7 represents findings N1 through N10 and keeps the test results provisional.
- Inspected the N1–N10 test names and ran the 28-test suite once. All tests passed.
- Ran `python tools/validate_repository.py --all` before this report was added. It exited 0 with zero errors and zero warnings.
- Rechecked the trace from specification sections 7 through 22 to implementation tasks, checkpoints, delivery gates, and stop conditions.
- Made no implementation or GitHub change.

After the plan passed, the evaluator changed only the task-list status from `Draft for validation` to `Validated` and changed the report target into a relative Markdown link. The task-list version stayed 0.3. The validated SHA-256 above is the hash after those two metadata changes.

## Scope

- [Repository-governance specification version 0.2](../../specs/repository-governance/spec.md)
- [Implementation task list version 0.3](../../plans/repository-governance/tasks.md)
- [Version 0.2 task-list validation](2026-09-04-02-task-list.md)
- [Failed implementation validation](2026-09-04-03-implementation.md)
- Task status, evidence identifiers, checkpoints, gates, and stop conditions
- The named G1–G15 and N1–N10 regression tasks

## Evidence

| Check | Result | Evidence |
| --- | --- | --- |
| Input identity | Pass | The draft hash matched the supplied SHA-256 exactly. |
| Final identity | Pass | The metadata-only final artifact has SHA-256 `f4fa90dd288aacfccccedf18a25a3098f5cbc6d4944a436bf629e8f16b6387d8`. |
| Task-table structure | Pass | All 45 rows have the required seven columns. |
| Dependency references | Pass | Each dependency names an existing task in the same list. |
| Dependency order | Pass | No task depends on a later task. No `Done` or `Ready` task has an incomplete dependency. |
| Current execution state | Pass | Tasks 0.1–0.5 and 1.1 are `Done`; task 1.2 is `Ready`; tasks 1.3 and 6.5 are `Blocked`; all other tasks are `Pending`; no task is `In progress`. |
| Blocked-work honesty | Pass | Local files and test runs after task 1.3 are described as prepared, earlier, local, or provisional. Their tasks stay `Pending`. They do not claim formal completion. |
| Exact identifiers | Pass | Completed task 1.1 identifies the repository, default branch, commit SHA, tree SHA, merged pull request, repository contents, permissions, UTC date, and read-only GitHub evidence source. Later tasks require exact branch SHAs, pull-request URL, workflow run ID and URL, rule ID, UTC time, commands, and exit codes before completion. |
| G1–G15 | Pass | Tasks 4.2–4.6 retain all original fixture and contract coverage. |
| N1–N10 | Pass | Task 4.7 rejects N1–N9, accepts N10, preserves G1–G15, requires two 28-test runs, and identifies the validator and test file outputs. All ten named regression tests exist. |
| Local test observation | Pass | `python -m unittest discover -s tests/repository_validation -v` ran 28 tests successfully during this review. This is supporting evidence only because task 4.7 is still `Pending`. |
| State synchronization | Pass | `STATE.md`, `BACKLOG.md`, and `ROADMAP.md` all show repository governance as blocked on live GitHub enforcement evidence. |
| External delivery control | Pass | Tasks 6.2–6.6 and the delivery gates require a feature branch, a ready pull request, exact workflow evidence, an active required rule, and independent revalidation before merge. |

## Findings

No plan-level defect remains in version 0.3.

The version correctly responds to V2. It gives the changed task list a new version and a new validation report. It also corrects the earlier status overstatement: all implementation work after the unresolved external checkpoint stays `Pending`, even when local output exists.

Version 0.3 does not claim that V1 or V3 is closed. Task 4.7 records provisional regression evidence for V1. Tasks 1.3, 6.4, 6.5, and 6.6 still require live GitHub evidence for V3.

## Specification traceability

| Specification area | Task-list coverage | Result |
| --- | --- | --- |
| Sections 7–8 — Structure and metadata | Tasks 1.4, 2.1–2.9, 3.1–3.3, and 5.1–5.3 | Pass |
| Sections 9–12 — Status, links, state, and roadmap | Tasks 2.1–2.9, 3.4–3.6, 4.7, and 6.7 | Pass |
| Sections 13–14 — Immutability and synchronization | Tasks 1.4, 3.7–3.8, 4.3, 4.5, 4.7, and the Evidence gate | Pass |
| Section 15 — Pull requests and merge | Tasks 6.1–6.7 and all delivery gates | Pass |
| Section 16 — Validator behavior | Tasks 3.1–3.9, 4.5, and 4.7 | Pass |
| Section 17 — Required tests | Tasks 4.1–4.7, including G1–G15 and N1–N10 | Pass |
| Sections 18–19 — CI and pull-request template | Tasks 5.1–5.4 and 6.3–6.4 | Pass |
| Section 20 — Migration | Tasks 1.4 and 2.1–2.9 | Pass |
| Section 21 — Failure behavior | Blocked tasks 1.3 and 6.5 plus the rollback and stop conditions | Pass |
| Section 22 — Acceptance | Checkpoints C–H, tasks 4.7 and 5.4, tasks 6.1–6.7, and all delivery gates | Pass |

## Residual risks

| Risk | Effect | Next control |
| --- | --- | --- |
| The workspace has no git metadata. | This revalidation cannot prove the branch base, candidate diff, or merge-base immutability behavior in the real repository. | Complete tasks 1.2, 3.8, 6.1, and 6.2 on the real feature branch. Run base/head validation there. |
| The N1–N10 correction evidence is provisional. | Passing local tests do not close implementation finding V1 while task 4.7 is `Pending`. | After tasks 1.3 through 4.6 are formally complete, run task 4.7 twice, record the exact commands and results, and close V1 only in report sequence 05. |
| Live branch-rule evidence and configuration are unavailable in this workspace. | Finding V3 remains open. The repository is not yet proven to block an unvalidated merge or direct push. | Keep governance `Blocked`. Complete tasks 1.3 and 6.4–6.6 with exact read-only GitHub evidence. Do not merge before the required rule is active. |
| Some pending evidence is historical. Task 6.1 still mentions the earlier 18-test run, and task 2.9 records the report count before this report. | A later worker could mistake provisional history for current completion evidence. | Before either task becomes `Done`, replace its evidence with the current report count, 28-test result, exact commands, UTC times, exit codes, and feature-branch diff evidence. |

## Decision

Task-list version 0.3 is valid for continued execution. It is `Validated`, not `Active` or `Complete`. V2 is closed at the task-list level. V1 still requires formal regression evidence, and V3 still requires live GitHub enforcement evidence. Start with task 1.2. Do not merge while either implementation finding remains open.
