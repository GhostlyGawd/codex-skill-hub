# Repository Governance — Task-List Validation

Version: 1.0  
Date: 2026-09-04  
Work ID: `repository-governance`  
Result: Pass with residual risk  
Evaluator: Independent Codex subagent

Validated artifact: [Implementation task list version 0.2](../../plans/repository-governance/tasks.md)  
Validated SHA-256: `e7b0b28471e98a0fd2b7c465acd889f8c1035dab080b96719bfe622cd317cd6c`

This report becomes immutable when it first enters the default branch. Task 1.4 will give it the canonical dated path before that event.

## Method

The evaluator compared every task, checkpoint, delivery gate, and stop condition with specification version 0.2 and its independent validation report. The review checked:

- Traceability to specification sections 7 through 22.
- Dependency order and current task status.
- Deterministic outputs and observable verification.
- Migration and status reconciliation.
- Validator checks and fixtures G1 through G15.
- Continuous integration behavior.
- Safe GitHub changes and branch-rule proof.
- Independent implementation validation.
- Pull-request, merge, post-merge, and rollback gates.

The evaluator also parsed all 44 task rows. The check found no unknown dependency, no forward dependency, no invalid `Ready` state, and no invalid `Done` state. Task 1.1 is the only `Ready` task. No task is `In progress`.

The local workspace has no git metadata. The evaluator did not inspect or change the live GitHub repository, pull requests, workflows, or branch rules. These actions belong to later implementation tasks.

## Result summary

The corrected task list passes. It gives each required implementation area an ordered task, a defined output, an observable check, and a stop or delivery control. No blocking finding remains.

The first review found four defects. Version 0.2 corrects them.

## Corrections

| ID | Finding in version 0.1 | Correction in version 0.2 | Result |
| --- | --- | --- | --- |
| T1 | The task-list validation output used its final canonical name before the planned normalization step. Task 1.4 normalized only the specification report. | Task 0.4 now uses the current requested report path. Task 1.4 normalizes both governance reports and all affected links before either report enters the default branch. | Closed |
| T2 | Several external and verification outputs were generic records without a stable path or identifier. | The tasks now store exact baseline fields, SHAs, URLs, run IDs, commands, times, exit codes, and matrices in named task `Evidence` cells. The feature branch name is also exact. | Closed |
| T3 | G7 and G8 did not state both required variants, and the implementation report did not explicitly require all specification residual risks. | Task 4.3 now requires both G7 and G8 variants. Task 6.6 now requires every residual risk in specification section 23. Migration-report evidence requirements are also explicit. | Closed |
| T4 | Branch-rule configuration did not prohibit weaker protection and had no exact recovery action. | Task 6.5 now requires the minimum narrow change, preserves stronger controls, records before-and-after evidence, and prohibits rule deletion or weakening. Stop conditions now define restoration and blocked-state behavior. | Closed |

## Specification traceability

| Specification area | Task-list coverage | Result |
| --- | --- | --- |
| Section 7 — Canonical structure | Tasks 1.4, 2.1–2.9, 3.1, and 5.1–5.3 | Pass |
| Section 8 — Metadata and required tables | Tasks 2.2–2.8, 3.2, and 3.3 | Pass |
| Section 9 — Status models | Tasks 2.4, 2.5, 2.9, 3.4–3.6, and 6.7 | Pass |
| Section 10 — Artifact links | Tasks 1.4, 2.4–2.9, 3.3, 3.5, and 6.7 | Pass |
| Section 11 — Current state | Tasks 2.1, 2.9, 3.5, and 6.7 | Pass |
| Section 12 — Roadmap | Tasks 2.2, 2.3, 2.9, 3.5, and 6.7 | Pass |
| Section 13 — Immutable reports | Tasks 1.3, 1.4, 3.8, 4.3, 6.6, and the Evidence gate | Pass |
| Section 14 — Change classification and synchronization | Tasks 3.7, 4.3, 4.5, 6.3, and 6.7 | Pass |
| Section 15 — Pull request and merge rules | Tasks 6.2–6.7 and all delivery gates | Pass |
| Section 16 — Repository validator | Tasks 3.1–3.9 and 5.4 | Pass |
| Section 17 — Fixtures G1–G15 | Tasks 4.1–4.6 | Pass |
| Section 18 — Continuous integration | Tasks 5.1, 5.4, 6.4, and the Default-branch gate | Pass |
| Section 19 — Pull-request template | Tasks 5.2 and 6.3 | Pass |
| Section 20 — Migration | Tasks 1.4 and 2.1–2.9 | Pass |
| Section 21 — Failure behavior | Rollback and stop conditions, tasks 2.1–2.9, 3.8, and 6.5 | Pass |
| Section 22 — Acceptance criteria | Checkpoints C–H, tasks 4.6, 5.4, and 6.1–6.7 | Pass |

## Requested validation areas

| Area | Result | Evidence |
| --- | --- | --- |
| Complete traceability | Pass | Every specification section from 7 through 22 maps to one or more tasks and a checkpoint or gate. |
| Dependency order | Pass | All 44 task rows use known earlier dependencies. The critical path agrees with the row dependencies. |
| Current statuses | Pass | Tasks 0.1–0.5 are `Done`; task 1.1 is `Ready`; later tasks are `Pending`; no task is incorrectly active. |
| Deterministic outputs | Pass | File outputs have exact paths. External objects and run results use specified identifiers in exact task evidence cells. |
| Observable checks | Pass | Every task has a direct file, command, status, API, SHA, link, check-ID, or rule-state verification. |
| Safe GitHub changes | Pass | Tasks limit pushes to the feature branch, prohibit overwriting unrelated work, require a ready pull request, and apply a narrow branch-rule change. |
| Branch-rule proof | Pass | Tasks 1.3, 6.4, 6.5, and 6.6 require the rule ID, branch, check name, SHAs, UTC time, and read-only inspection source. |
| Validator fixtures G1–G15 | Pass | Tasks 4.2–4.4 cover each fixture and its expected exit result. Task 4.3 covers both G7 and G8 variants. |
| Migration | Pass | Tasks 1.4 and 2.1–2.9 cover all current external and repository artifacts, historical context, and new migration evidence. |
| State, backlog, and roadmap reconciliation | Pass | Tasks 2.1–2.3, 2.9, and 6.7 require derived status agreement and a complete matrix. |
| Continuous integration | Pass | Task 5.1 includes both event paths, full history, stable check name, read-only permissions, no stored secret, and concurrency cancellation. |
| Final validation | Pass | Task 6.6 requires independent validation of the exact implementation tree and active branch-rule evidence. |
| Pull-request and merge gates | Pass | Tasks 6.3–6.7 and the five delivery gates prevent a draft, unrelated scope, absent check, stale head, or unverified merge. |
| Rollback conditions | Pass | The list stops unsafe delivery, preserves immutable evidence and user work, restores an incorrect rule change, and uses a new ready pull request for a merged revert. |

## Residual risks

| Risk | Effect | Next control |
| --- | --- | --- |
| The workspace is not a git checkout. | This validation cannot prove the live default branch, current open pull requests, or existing protection rules. | Complete tasks 1.1 through 1.3 before migration or mutation. Record the exact GitHub evidence. |
| GitHub permission or plan limits can prevent branch-rule configuration. | Repository governance will not be fully enforced even if repository files and CI are correct. | Task 6.5 and its stop condition must set the work to `Blocked` and name the required administrator action. Do not merge. |
| The fixed report dates are valid on 2026-09-04 UTC only. | A report created after the UTC date changes can have incorrect metadata or a misleading filename. | Before each report is created, verify the UTC date. If it changed, update the output path, metadata, and links in the same task-list revision. |

## Decision

Task-list version 0.2 is valid for implementation. No blocker remains. Implementation must start with task 1.1 and must stop at each checkpoint when its conditions do not pass.
